Design a responsive 3-column dashboard using CSS Grid + Subgrid. Include:

- Auto-placement, `minmax()` track sizing, `gap` handling
- Mobile fallback strategy
- Containing block resolution notes
- Progressive enhancement with `@supports`
