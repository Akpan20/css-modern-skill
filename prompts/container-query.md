Build a responsive component using `@container`. Include:

- `container-type: inline-size`
- Fallback for unsupported browsers
- `text-wrap: balance` for typography
- Note containing block behavior & progressive enhancement
